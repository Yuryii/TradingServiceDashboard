@model IEnumerable<Dashboard.Models.ViewModels.CustomerListVM>
@{
    Layout = "~/Views/Shared/_ContentNavbarLayout.cshtml";
    ViewData["Title"] = "Customers";
}

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h5 class="mb-0">Customers</h5>
                <div class="d-flex gap-1">
                    <a asp-action="Customers_Export" class="btn btn-success btn-sm" title="Export Excel">
                        <i class="icon-base bx bx-download me-1"></i> Export
                    </a>
                    <button type="button" class="btn btn-info btn-sm" data-bs-toggle="modal" data-bs-target="#excelImportModal-Customer" title="Import Excel">
                        <i class="icon-base bx bx-upload me-1"></i> Import
                    </button>
                    <a asp-action="Customers_Create" class="btn btn-primary btn-sm">
                        <i class="icon-base bx bx-plus me-1"></i> Create New
                    </a>
                </div>
            </div>
            <div class="table-responsive text-nowrap">
                <table class="table">
                    <thead>
                        <tr>
                            <th>Customer Code</th>
                            <th>Customer Name</th>
                            <th>Type</th>
                            <th>Customer Group</th>
                            <th>Phone</th>
                            <th>Email</th>
                            <th>Status</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody class="table-border-bottom-0">
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td><strong>@item.CustomerCode</strong></td>
                                <td>@item.CustomerName</td>
                                <td>@item.CustomerType</td>
                                <td>@item.CustomerGroupName</td>
                                <td>@item.Phone</td>
                                <td>@item.Email</td>
                                <td>
                                    @if (item.IsActive)
                                    {
                                        <span class="badge bg-label-success">Active</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-label-secondary">Inactive</span>
                                    }
                                </td>
                                <td>
                                    <div class="dropdown">
                                        <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
                                            <i class="icon-base bx bx-dots-vertical-rounded"></i>
                                        </button>
                                        <div class="dropdown-menu">
                                            <a class="dropdown-item" asp-action="Customers_Edit" asp-route-id="@item.CustomerID">
                                                <i class="icon-base bx bx-edit-alt me-1"></i> Edit
                                            </a>
                                            <form asp-action="Customers_Delete" asp-route-id="@item.CustomerID" method="post" onsubmit="return confirm('Are you sure you want to delete this customer?');" style="display:inline;">
                                                @Html.AntiForgeryToken()
                                                <button type="submit" class="dropdown-item text-danger">
                                                    <i class="icon-base bx bx-trash me-1"></i> Delete
                                                </button>
                                            </form>
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@{
    var customerModal = new Dashboard.Models.ViewModels.ExcelImportModalVM
    {
        ModalId = "Customer",
        EntityType = "Customer",
        EntityName = "Customer",
        EntityDisplayName = "Khach hang"
    };
}
<partial name="~/Views/Shared/Partials/_ExcelImportModal.cshtml" model="customerModal" />
