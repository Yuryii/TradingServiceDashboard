@model Dashboard.Models.ViewModels.EmployeeCreateVM
@{
    Layout = "~/Views/Shared/_ContentNavbarLayout.cshtml";
    ViewData["Title"] = "Create Employee";
}

<div class="row">
  <div class="col-12 col-lg-10">
    <div class="card mb-6">
      <div class="card-header">
        <h5 class="mb-0">Create New Employee</h5>
      </div>
      <div class="card-body pt-4">
        @if (ViewData.ModelState.ErrorCount > 0)
        {
          <div class="alert alert-danger" role="alert">
            <div asp-validation-summary="ModelOnly"></div>
          </div>
        }

        <form asp-action="Employees_Create" method="post">
          @Html.AntiForgeryToken()

          <h6 class="mb-4">Basic Information</h6>
          <div class="row g-6">
            <div class="col-md-6">
              <label asp-for="EmployeeCode" class="form-label">Employee Code</label>
              <input asp-for="EmployeeCode" class="form-control" placeholder="EMP-001" />
              <span asp-validation-for="EmployeeCode" class="text-danger"></span>
            </div>
            <div class="col-md-6">
              <label asp-for="FullName" class="form-label">Full Name</label>
              <input asp-for="FullName" class="form-control" placeholder="John Doe" />
              <span asp-validation-for="FullName" class="text-danger"></span>
            </div>
            <div class="col-md-4">
              <label asp-for="Gender" class="form-label">Gender</label>
              <select asp-for="Gender" class="form-select">
                <option value="">-- Select --</option>
                <option value="Male">Male</option>
                <option value="Female">Female</option>
              </select>
            </div>
            <div class="col-md-4">
              <label asp-for="DateOfBirth" class="form-label">Date of Birth</label>
              <input asp-for="DateOfBirth" class="form-control" type="date" />
            </div>
            <div class="col-md-4">
              <label asp-for="Phone" class="form-label">Phone</label>
              <input asp-for="Phone" class="form-control" placeholder="+1 202 555 0111" />
            </div>
            <div class="col-md-6">
              <label asp-for="Email" class="form-label">Email</label>
              <input asp-for="Email" class="form-control" type="email" placeholder="john@example.com" />
              <span asp-validation-for="Email" class="text-danger"></span>
            </div>
            <div class="col-md-6">
              <label asp-for="AddressLine" class="form-label">Address</label>
              <input asp-for="AddressLine" class="form-control" placeholder="123 Main St" />
            </div>
          </div>

          <hr class="my-6 mx-n4" />
          <h6 class="mb-4">Employment Details</h6>
          <div class="row g-6">
            <div class="col-md-6">
              <label asp-for="DepartmentID" class="form-label">Department</label>
              <select asp-for="DepartmentID" class="form-select">
                <option value="">-- Select Department --</option>
                @if (ViewBag.Departments != null)
                {
                  @foreach (var dept in ViewBag.Departments)
                  {
                    <option value="@dept.DepartmentID">@dept.DepartmentName</option>
                  }
                }
              </select>
              <span asp-validation-for="DepartmentID" class="text-danger"></span>
            </div>
            <div class="col-md-6">
              <label asp-for="PositionID" class="form-label">Position</label>
              <select asp-for="PositionID" class="form-select">
                <option value="">-- Select Position --</option>
                @if (ViewBag.Positions != null)
                {
                  @foreach (var pos in ViewBag.Positions)
                  {
                    <option value="@pos.PositionID">@pos.PositionName</option>
                  }
                }
              </select>
              <span asp-validation-for="PositionID" class="text-danger"></span>
            </div>
            <div class="col-md-6">
              <label asp-for="BranchID" class="form-label">Branch</label>
              <select asp-for="BranchID" class="form-select">
                <option value="">-- Select Branch --</option>
                @if (ViewBag.Branches != null)
                {
                  @foreach (var branch in ViewBag.Branches)
                  {
                    <option value="@branch.BranchID">@branch.BranchName</option>
                  }
                }
              </select>
            </div>
            <div class="col-md-6">
              <label asp-for="ManagerID" class="form-label">Manager</label>
              <select asp-for="ManagerID" class="form-select">
                <option value="">-- Select Manager --</option>
                @if (ViewBag.Managers != null)
                {
                  @foreach (var mgr in ViewBag.Managers)
                  {
                    <option value="@mgr.EmployeeID">@mgr.FullName</option>
                  }
                }
              </select>
            </div>
            <div class="col-md-6">
              <label asp-for="EmploymentType" class="form-label">Employment Type</label>
              <select asp-for="EmploymentType" class="form-select">
                <option value="FullTime">Full Time</option>
                <option value="PartTime">Part Time</option>
                <option value="Contract">Contract</option>
                <option value="Intern">Intern</option>
              </select>
              <span asp-validation-for="EmploymentType" class="text-danger"></span>
            </div>
            <div class="col-md-6">
              <label asp-for="HireDate" class="form-label">Hire Date</label>
              <input asp-for="HireDate" class="form-control" type="date" />
              <span asp-validation-for="HireDate" class="text-danger"></span>
            </div>
            <div class="col-md-6">
              <label asp-for="BaseSalary" class="form-label">Base Salary</label>
              <input asp-for="BaseSalary" class="form-control" type="number" step="0.01" placeholder="0.00" />
              <span asp-validation-for="BaseSalary" class="text-danger"></span>
            </div>
          </div>

          <div class="mt-6 d-flex gap-3">
            <button type="submit" class="btn btn-primary">
              <i class="icon-base bx bx-save me-1"></i> Create Employee
            </button>
            <a asp-action="Employees_Index" class="btn btn-outline-secondary">
              <i class="icon-base bx bx-x me-1"></i> Cancel
            </a>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>
