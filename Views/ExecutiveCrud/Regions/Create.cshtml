@model Dashboard.Models.ViewModels.RegionCreateVM
@{
    Layout = "~/Views/Shared/_ContentNavbarLayout.cshtml";
    ViewData["Title"] = "Create Region";
}

<div class="row">
  <div class="col-12 col-lg-8">
    <div class="card mb-6">
      <div class="card-header">
        <h5 class="mb-0">Create New Region</h5>
      </div>
      <div class="card-body pt-4">
        @if (ViewData.ModelState.ErrorCount > 0)
        {
          <div class="alert alert-danger" role="alert">
            <div asp-validation-summary="ModelOnly"></div>
          </div>
        }

        <form asp-action="Regions_Create" method="post">
          @Html.AntiForgeryToken()

          <div class="row g-6">
            <div class="col-md-6">
              <label asp-for="RegionCode" class="form-label">Region Code</label>
              <input asp-for="RegionCode" class="form-control" placeholder="e.g. R001" />
              <span asp-validation-for="RegionCode" class="text-danger"></span>
            </div>
            <div class="col-md-6">
              <label asp-for="RegionName" class="form-label">Region Name</label>
              <input asp-for="RegionName" class="form-control" placeholder="e.g. North America" />
              <span asp-validation-for="RegionName" class="text-danger"></span>
            </div>
            <div class="col-12">
              <label asp-for="Description" class="form-label">Description</label>
              <textarea asp-for="Description" class="form-control" rows="3" placeholder="Enter description"></textarea>
              <span asp-validation-for="Description" class="text-danger"></span>
            </div>
            <div class="col-md-6">
              <label asp-for="IsActive" class="form-label">Status</label>
              <div class="form-check form-switch mb-2">
                <input asp-for="IsActive" class="form-check-input" type="checkbox" checked />
                <label class="form-check-label" for="IsActive">Active</label>
              </div>
            </div>
          </div>

          <div class="mt-6 d-flex gap-3">
            <button type="submit" class="btn btn-primary">
              <i class="icon-base bx bx-save me-1"></i> Create Region
            </button>
            <a asp-action="Regions_Index" class="btn btn-outline-secondary">
              <i class="icon-base bx bx-x me-1"></i> Cancel
            </a>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>
